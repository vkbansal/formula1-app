{
  "name": "formula1-app",
  "version": "1.0.0",
  "description": "An app for Formula 1 world Championship related statistics",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .ts --ext .tsx src",
    "fmt": "prettier --write 'src/**/*.{ts,tsx,scss}'",
    "start": "rm -rf public && npx @11ty/eleventy --watch --serve --port=3000",
    "build": "rm -rf public && npx @11ty/eleventy"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "mongodb": "^4.5.0"
  },
  "devDependencies": {
    "eslint": "^8.14.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "^2.6.2"
  }
}
