{
  "name": "formula1-app",
  "version": "1.0.0",
  "description": "An app for Formula 1 world Championship related statistics",
  "main": "index.js",
  "scripts": {
    "lint": "eslint --ext .ts --ext .tsx src",
    "fmt": "prettier --write 'src/**/*.{ts,tsx,scss}'",
    "clean": "rm -rf public",
    "prestart": "yarn run clean",
    "start": "concurrently 'npm:start:*'",
    "start:assets": "gulp watch",
    "start:server": "npx @11ty/eleventy --serve",
    "prebuild": "yarn run clean",
    "build": "NODE_ENV=production gulp build && npx @11ty/eleventy",
    "datagen": "node ./datagen/index.mjs"
  },
  "author": "Vivek Kumar Bansal <contact@vkbansal.me>",
  "license": "MIT",
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "@vkbansal/gulp-plugins": "^0.3.0",
    "change-case": "^4.1.2",
    "concurrently": "^7.2.2",
    "date-fns": "^2.28.0",
    "globby": "^13.1.2",
    "gulp": "^4.0.2",
    "js-yaml": "^4.1.0",
    "merge-stream": "^2.0.0",
    "mongodb": "^4.7.0",
    "normalize.css": "^8.0.1",
    "sass": "^1.52.3"
  },
  "devDependencies": {
    "eslint": "^8.18.0",
    "eslint-config-prettier": "^8.5.0",
    "prettier": "^2.7.1"
  }
}
